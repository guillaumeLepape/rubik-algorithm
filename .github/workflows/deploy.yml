name: 📦 Deploy

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: deployment

    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: 3.13
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Configure Git Credentials
        run: |
          git config user.name "${{ secrets.USER_NAME }}"
          git config user.email "${{ secrets.USER_MAIL }}"

      - name: Build website
        run: uv run mkdocs build

      - name: Deploy to release/main
        run: |-
          git fetch origin release/main
          export RELEASE_FOLDER=/tmp/release/$(date +%s)
          mkdir $RELEASE_FOLDER
          cp -r site $RELEASE_FOLDER/site
          cp docker-compose.yml $RELEASE_FOLDER/docker-compose.yml
          git switch release/main
          cp $RELEASE_FOLDER/docker-compose.yml docker-compose.yml
          cp -r $RELEASE_FOLDER/site site
          git add -A
          git commit -m "Deploy docs"
          git push origin HEAD:release/main --force
